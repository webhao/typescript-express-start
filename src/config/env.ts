
export const ENVIRONMENT = process.env.NODE_ENV;
export const PORT = 3000;

// if (!ENVIRONMENT) {
//   logger.error("No node environment. Set NODE_ENV environment variable.");
//   process.exit(1);
// }
